---
en:
  scheduled_emails:
    index:
      title: Scheduled Emails
      new: New scheduled email
      empty_title: No scheduled emails
      empty_description: Schedule an email to be sent later — one time or recurring.
      to: "To %{address}"
      scheduled_for: "Scheduled for %{time}"
      recurring: Recurring
    new:
      title: Schedule Email
    create:
      created: Email scheduled successfully.
    edit:
      title: Edit Scheduled Email
    update:
      updated: Scheduled email updated.
    destroy:
      cancelled: Scheduled email cancelled.
    show:
      title: Scheduled Email
      back: ← Back to scheduled emails
      edit: Edit
      cancel: Cancel schedule
      confirm_cancel: Cancel this scheduled email?
      from: "From"
      scheduled: Scheduled
      recurring_label: "Repeats: %{rule}"
      status: Status
      preview: Preview
    form:
      errors:
        one: "1 error prevented this scheduled email from being saved:"
        other: "%{count} errors prevented this scheduled email from being saved:"
      select_account: Select an email account…
      body_placeholder: Write your email body here. Use {{ contact.first_name }} for template variables.
      rrule_hint: "Leave empty for one-time. Examples: FREQ=DAILY, FREQ=WEEKLY;INTERVAL=1, FREQ=MONTHLY;INTERVAL=1"
      template_context: Template variables
      template_context_hint: 'JSON object with Liquid template variables, e.g. {"contact": {"first_name": "John"}}'
      cancel: Cancel
  email_compose:
    create:
      scheduled: Email scheduled! You can manage it in Scheduled Emails.
      schedule_failed: Could not schedule email. Please check the required fields.
    compose_area:
      schedule: Schedule
  activerecord:
    attributes:
      scheduled_email:
        to_address: To
        cc_address: Cc
        bcc_address: Bcc
        subject: Subject
        body: Body
        scheduled_at: Send at
        rrule: Recurrence
        email_account_id: From
    models:
      scheduled_email:
        one: Scheduled Email
        other: Scheduled Emails
  activemodel:
    enums:
      scheduled_email:
        statuses:
          pending: Pending
          sent: Sent
          cancelled: Cancelled
          failed: Failed
