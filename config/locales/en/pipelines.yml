---
en:
  navigation:
    settings:
      items:
        pipelines: Pipelines
  settings:
    pipelines:
      create:
        created: Pipeline created.
      destroy:
        deleted: Pipeline deleted.
      edit:
        heading: Edit pipeline
        subtitle: Modify stages or pipeline settings.
      form:
        add_stage: Add stage
        applies_to_options:
          both: Both
          documents: Documents
          emails: Emails
        description_placeholder: What this pipeline tracks
        name_placeholder: e.g. Invoice Approval
        stages_heading: Stages
        stages_hint: Drag to reorder stages. Items in terminal stages cannot be moved.
      index:
        confirm_delete: Delete this pipeline? All memberships will be removed.
        create_first: Create your first pipeline
        empty_description: Create a pipeline to track documents or emails through
          custom stages like a kanban board.
        empty_title: No pipelines yet
        heading: Pipelines
        new_pipeline: New pipeline
        subtitle: Create custom workflows to track documents through stages.
        view_board: View board
      new:
        heading: New pipeline
        subtitle: Define the stages your documents will move through.
      update:
        updated: Pipeline updated.
