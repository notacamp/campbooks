---
en:
  # The Tasks module retired into asks (Now + Time); its views, board, skim, form,
  # discussions and controller flashes are gone. Only the model's enum/attribute
  # labels stay — they back human_enum / human_attribute_name for the Task record,
  # which the API and MCP tools still expose.
  activerecord:
    attributes:
      task:
        all_day: All day
        description: Description
        due_at: Due date
        priorities:
          high: High
          low: Low
          normal: Normal
          urgent: Urgent
        priority: Priority
        status: Status
        statuses:
          blocked: Blocked
          cancelled: Cancelled
          done: Done
          in_progress: In progress
          suggested: Suggested
          todo: To do
        title: Title
      task_email_link:
        relationships:
          blocked_by: Blocked by
          follow_up: Follow-up
          reference: Reference
          related: Related
    models:
      task:
        one: Task
        other: Tasks
