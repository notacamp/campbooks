---
en:
  # Composer-facing picker list (app/views/email_templates/index.html.erb) and
  # the variables fill form (fill_form.html.erb), both rendered into the picker.
  email_templates:
    index:
      empty: No email templates yet. Create one in Settings → Email templates.
      use: Use
      variables:
        one: "1 variable"
        other: "%{count} variables"
      documents:
        one: "1 document attached"
        other: "%{count} documents attached"
    fill_form:
      title: "Fill in: %{name}"
      subtitle: Fill the fields, then insert the template into your email.
      no_variables: This template is ready to insert.
      insert: Insert into email
      back: Back
      attachments_note:
        one: 1 document will be attached as a PDF.
        other: "%{count} documents will be attached as PDFs."
  settings:
    email_templates:
      index:
        title: Email templates
        subtitle: Create reusable emails — subject, body, and attached documents — to use from the composer.
        new_template: New template
        edit: Edit
        delete: Delete
        delete_confirm: Delete this template?
        empty_title: No templates yet
        empty_description: Create your first email template and reuse it whenever you compose.
        variables_count:
          one: "1 variable"
          other: "%{count} variables"
        documents_count:
          one: "1 document"
          other: "%{count} documents"
      new:
        title: New email template
        subtitle: Write the email once and reuse it — or let AI draft it from a description.
        save: Create template
        cancel: Cancel
      edit:
        title: "Edit: %{name}"
        subtitle: Review and refine the template, attach documents, or regenerate it with AI.
        save: Save changes
        cancel: Back to templates
        preview_heading: Preview
        preview_subject: Subject
        variables_heading: Variables
        variable_key: Key
        variable_label: Label
        variable_type: Type
        variable_required: Required
        generate_heading: AI generation
        generate_description: Let AI draft the subject and body from your description.
        generate_button: Generate with AI
      create: { created: Template created. }
      update: { updated: Template updated. }
      destroy: { destroyed: Template deleted. }
      regenerate: { generating: AI generation started. }
      form:
        name_placeholder: e.g. Welcome email
        description_hint: Describe the email you want AI to draft (used only for AI generation).
        description_placeholder: e.g. A friendly welcome email for new clients with next steps.
        subject_hint: You can use {{ variable_name }} for dynamic content.
        subject_placeholder: e.g. Welcome to {{ workspace_name }}, {{ recipient_name }}!
        body_label: Body
        body_hint: Use {{ variable_name }} anywhere for dynamic content.
        body_placeholder: Write your email…
        documents_label: Attached documents
        documents_hint: These document templates are filled with the same variables and attached as PDFs.
        documents_empty: No ready document templates yet. Create one in Document templates first.
