<%= render(Campbooks::Compose::Dock.new(
  mode: mode,
  message: message,
  draft: draft,
  to: to,
  cc: cc,
  bcc: bcc,
  subject: subject,
  body: body,
  quoted_body: quoted_body,
  signatures: signatures,
  signature_id: signature_id,
  accounts: accounts,
  attachment_entries: attachment_entries,
  scout_draft: local_assigns[:scout_draft]
)) %>
