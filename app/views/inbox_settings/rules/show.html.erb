<%# Renders the rule row (the partial provides its own turbo frame) so the
    run-progress poller can refresh a single row without reloading the list. %>
<%= render "inbox_settings/rules/rule", rule: @rule %>
