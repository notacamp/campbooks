<%# Renders the rule row in its own frame so the run-progress poller can
    refresh a single row without reloading the whole list. %>
<%= turbo_frame_tag dom_id(@rule) do %>
  <%= render "inbox_settings/rules/rule", rule: @rule %>
<% end %>
