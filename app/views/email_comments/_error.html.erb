<%# Shown in place of the typing dots when Scout's reply fails or comes back
    empty. Padding matches a normal comment row so it sits cleanly in the list. %>
<div class="px-4 py-3">
  <%= render(Campbooks::ChatError.new(retry_content: local_assigns[:retry_content])) %>
</div>
