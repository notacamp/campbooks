<div class="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3">
  <% Digests::Presets::ALL.each do |preset| %>
    <%= render Campbooks::Digests::PresetCard.new(preset: preset) %>
  <% end %>
</div>
