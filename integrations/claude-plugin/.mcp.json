{
  "mcpServers": {
    "campbooks": {
      "type": "http",
      "url": "${user_config.server_url}/api/mcp",
      "headers": {
        "Authorization": "Bearer ${user_config.mcp_key}"
      }
    }
  }
}
