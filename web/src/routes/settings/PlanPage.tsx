/**
 * PlanPage — Settings → Plan at /settings/plan.
 * Stub — not yet implemented. Backed by GET /api/app/settings/plan.
 */
import { type FC } from "react";
import { StubPage } from "./StubPage";

export const PlanPage: FC = () => (
  <StubPage
    title="Plan"
    description="View your current plan, usage, and billing details."
  />
);
