/**
 * MembersPage — Settings → Members at /settings/members.
 * Stub — not yet implemented. Backed by GET/PATCH /api/app/settings/members
 * and POST /api/app/settings/invitations.
 */
import { type FC } from "react";
import { StubPage } from "./StubPage";

export const MembersPage: FC = () => (
  <StubPage
    title="Members"
    description="Manage team members, invite colleagues, and control who has access to this workspace."
  />
);
