/**
 * InboxSettingsPage — Settings → Inbox settings at /settings/inbox.
 * Stub — not yet implemented. Backed by the /api/app/inbox_settings/* namespace
 * (tags, rules, signatures, document_types, tag_groups, filtering).
 */
import { type FC } from "react";
import { StubPage } from "./StubPage";

export const InboxSettingsPage: FC = () => (
  <StubPage
    title="Inbox settings"
    description="Manage tags, inbox rules, email signatures, document types, and filtering preferences."
  />
);
