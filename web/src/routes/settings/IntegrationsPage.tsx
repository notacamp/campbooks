/**
 * IntegrationsPage — Settings → Integrations at /settings/integrations.
 * Stub — not yet implemented. Backed by /api/app/settings/integrations/*
 * (notion, calendars, connections).
 */
import { type FC } from "react";
import { StubPage } from "./StubPage";

export const IntegrationsPage: FC = () => (
  <StubPage
    title="Integrations"
    description="Connect Google Drive, Notion, and other services. Manage API connections used by Scout and workflows."
  />
);
