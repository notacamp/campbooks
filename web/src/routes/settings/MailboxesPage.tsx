/**
 * MailboxesPage — Settings → Mailboxes at /settings/mailboxes.
 * Stub — reserved for the Mailboxes agent. Do NOT implement here.
 * The stub keeps the nav entry functional and the registry extensible.
 */
import { type FC } from "react";
import { StubPage } from "./StubPage";

export const MailboxesPage: FC = () => (
  <StubPage
    title="Mailboxes"
    description="Connect and manage email accounts — Google, Zoho, Microsoft, and IMAP providers."
  />
);
