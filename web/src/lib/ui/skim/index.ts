/**
 * skim — barrel for the Skim overlay component.
 *
 * The parent lib/ui barrel (index.ts) does NOT import this yet — it is owned
 * by another session. Add the following line there when ready:
 *   export { Skim, type SkimCard, type SkimAction, type SkimProps } from "./skim/skim";
 */
export { Skim, type SkimCard, type SkimAction, type SkimProps } from "./skim";
